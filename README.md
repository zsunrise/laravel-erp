# Laravel ERP 系统

<div align="center">

![Laravel](https://img.shields.io/badge/Laravel-9.x-FF2D20?style=for-the-badge&logo=laravel&logoColor=white)
![Vue](https://img.shields.io/badge/Vue-3.x-4FC08D?style=for-the-badge&logo=vue.js&logoColor=white)
![PHP](https://img.shields.io/badge/PHP-8.0+-777BB4?style=for-the-badge&logo=php&logoColor=white)
![License](https://img.shields.io/badge/License-MIT-green?style=for-the-badge)

**一个功能完整的企业资源规划（ERP）系统**

[功能特性](#-核心功能模块) • [快速开始](#-快速开始) • [API 文档](#-api-接口文档) • [部署指南](./DEPLOYMENT.md)

</div>

---

## 📖 项目简介

Laravel ERP 是一个基于 Laravel 9 和 Vue 3 开发的企业资源规划系统，提供完整的业务流程管理功能。系统采用前后端分离架构，后端提供 RESTful API，前端使用 Vue 3 + Element Plus 构建现代化的单页应用（SPA）。

### ✨ 核心特色

- 🎯 **功能完整**：涵盖采购、销售、库存、生产、财务等 15+ 个核心业务模块
- 🔐 **权限完善**：基于 RBAC 的权限管理系统，所有接口都有权限验证
- 🎨 **界面统一**：采用现代化的 UI 设计规范，提供一致的用户体验
- 📊 **报表丰富**：提供销售、采购、库存、财务等多维度报表分析
- 🔄 **流程灵活**：支持自定义审批流程，适应不同业务场景
- ⚡ **性能优化**：数据库索引优化，避免 N+1 查询问题
- 📱 **响应式设计**：支持多设备访问，适配桌面和移动端

## 🛠️ 技术栈

### 后端技术

| 技术 | 版本 | 说明 |
|------|------|------|
| **Laravel** | 9.x | PHP Web 框架 |
| **PHP** | 8.0.2+ | 编程语言 |
| **Laravel Sanctum** | 3.x | API 认证 |
| **MySQL/MariaDB** | 5.7+ | 数据库 |
| **Scribe** | 4.x | API 文档生成 |

### 前端技术

| 技术 | 版本 | 说明 |
|------|------|------|
| **Vue** | 3.x | 前端框架（Composition API） |
| **Element Plus** | 2.x | UI 组件库 |
| **Pinia** | 2.x | 状态管理 |
| **Vue Router** | 4.x | 路由管理 |
| **Vite** | 4.x | 构建工具 |
| **Lucide Vue Next** | 0.x | 图标库 |
| **ECharts** | 6.x | 图表库 |
| **Tailwind CSS** | 4.x | CSS 框架 |

## 📦 核心功能模块

### 0. 仪表盘模块 📊
- ✅ 数据统计展示（销售订单、采购订单、库存、营收、待审批、工单、应收应付账款）
- ✅ 环比趋势分析（销售订单、采购订单、营收的月度环比增长率）
- ✅ 销售趋势图表（近7天销售趋势可视化）
- ✅ 待处理事项（待审批工作流、低库存预警、逾期应收账款）
- ✅ 最近订单列表（最近销售订单和采购订单）
- ✅ 快捷操作入口（快速跳转到常用功能）

### 1. 系统管理模块 🔧
- ✅ 用户管理（CRUD、角色分配、状态管理）
- ✅ 角色权限管理（RBAC 结构，支持多角色）
- ✅ 权限管理（细粒度权限控制）
- ✅ 系统配置管理（键值对配置）
- ✅ 数据字典管理（支持分类和类型）
- ✅ 消息模板管理（前端页面配置，支持多渠道、多类型）
- ✅ 操作日志记录（完整的操作审计）
  - ✅ 自动记录所有 API 操作（POST、PUT、PATCH、DELETE）
  - ✅ 记录用户信息、操作模块、操作动作、请求数据、响应数据
  - ✅ 记录 IP 地址、用户代理、状态码等详细信息
  - ✅ 自动排除敏感信息（密码、token 等显示为 `***`）
  - ✅ 自动排除查询操作（GET 请求）以提升性能
  - ✅ 完整记录请求和响应数据（不截断，JSON 格式化显示）
  - ✅ 前端页面查询和管理（系统管理 → 操作日志）
  - ✅ 支持按用户、模块、操作、请求方法、状态码、日期范围等条件查询
  - ✅ 支持关键词搜索（操作、路径、用户等）
  - ✅ 详情查看（包含完整的请求数据和响应数据，自动换行显示）

### 2. 基础数据模块 📋
- ✅ 商品分类管理（树形结构，支持多级分类）
- ✅ 计量单位管理
- ✅ 币种管理
- ✅ 地区管理（省市区三级联动）
- ✅ 数据字典（系统级配置数据）

### 3. 商品管理模块 🛍️
- ✅ 商品信息管理（SKU、条形码、价格、库存预警）
- ✅ 商品搜索和筛选（多条件组合查询）
- ✅ 商品分类关联

### 4. 仓库管理模块 🏭
- ✅ 仓库信息管理（多仓库支持）
- ✅ 库位管理（仓库内库位管理）

### 5. 库存管理模块 📦
- ✅ 库存查询（多条件筛选、低库存预警）
- ✅ 入库管理（采购入库、生产入库、调拨入库）
- ✅ 出库管理（销售出库、生产领料、调拨出库）
- ✅ 库存调拨（仓库间调拨）
- ✅ 库存调整（盘点调整）
- ✅ 盘点管理（盘点计划、盘点执行、差异处理、自动调整）
- ✅ 库存交易记录查询（完整的库存变动历史）

### 6. 供应商管理模块 🏢
- ✅ 供应商档案管理（评级、信用额度、账期等）
- ✅ 供应商评级管理

### 7. 客户管理模块 👥
- ✅ 客户档案管理（评级、信用额度、账期等）
- ✅ 客户评级管理

### 8. 采购管理模块 🛒
- ✅ 采购订单管理（创建、编辑、审核、取消）
- ✅ 采购入库（与库存管理集成）
- ✅ 采购退货（退货单管理、审核）
- ✅ 采购对账与结算（对账单、结算单、付款）

### 9. 销售管理模块 💰
- ✅ 销售订单管理（创建、编辑、审核、取消）
- ✅ 销售出库（与库存管理集成，自动检查库存）
- ✅ 销售退货（退货单管理、自动入库）
- ✅ 销售对账与收款（对账单、结算单、收款）

### 10. BOM 管理模块 🔧
- ✅ 物料清单管理（创建、编辑、删除、复制、版本管理）
- ✅ 工艺路线管理（创建、编辑、删除、复制、版本管理）
- ✅ BOM 成本计算
- ✅ 工艺路线总工时计算

### 11. 生产管理模块 🏭
- ✅ 生产计划管理（创建、编辑、审核、删除）
- ✅ 工单管理（创建、编辑、审核、派工、进度跟踪）
- ✅ 生产领料（与库存管理集成，自动检查库存）
- ✅ 生产退料（自动入库）
- ✅ 生产报工（工时、产量、合格率）
- ✅ 生产入库（工单完成时自动入库）

### 12. 财务管理模块 💳
- ✅ 会计科目管理（支持树形结构）
- ✅ 会计凭证管理（创建、编辑、删除、过账、借贷平衡检查）
- ✅ 总账查询（科目余额查询）
- ✅ 应收账款管理（创建、收款、账龄分析）
- ✅ 应付账款管理（创建、付款）
- ✅ 成本分配管理
- ✅ 财务报表（资产负债表、利润表）

### 13. 审批流程模块 ✅
- ✅ 流程设计（创建、编辑、删除流程和节点）
- ✅ 流程启动（关联业务单据）
- ✅ 审批功能（通过、拒绝、转交）
- ✅ 待审批列表
- ✅ 审批历史记录

### 14. 报表分析模块 📊
- ✅ 销售报表（销售统计、客户分析、产品分析、趋势分析）
- ✅ 采购报表（采购统计、供应商分析、产品分析、趋势分析）
- ✅ 库存报表（库存周转率、呆滞料分析、库存估值、库存变动）
- ✅ 财务报表（资产负债表、利润表）
- ✅ 自定义报表（报表定义、报表执行、动态查询）

### 15. 消息通知模块 📬
- ✅ 系统消息（发送、接收、阅读、删除）
- ✅ 消息模板管理（创建、编辑、删除、预览）
  - ✅ 前端页面配置（通过系统管理菜单访问）
  - ✅ 支持多种消息类型（系统消息、审批、订单、库存、财务）
  - ✅ 支持多种发送渠道（系统内消息、邮件、短信、推送）
  - ✅ 模板变量支持（使用 `{变量名}` 格式，支持动态替换）
  - ✅ 模板预览功能（支持测试数据预览）
  - ✅ 模板启用/禁用控制
- ✅ 邮件通知（集成 Laravel Mail）
- ✅ 短信通知（预留接口，需集成短信服务商）
- ✅ 推送通知（预留接口，需集成推送服务）
- ✅ 批量发送（发送给多个用户、按角色发送）
- ✅ 未读消息统计
- ✅ 消息发送日志（记录所有发送渠道的发送状态）

### 16. 个人中心模块 👤
- ✅ 个人信息管理（头像、姓名、邮箱、手机号）
- ✅ 密码修改
- ✅ 角色查看（查看当前用户所属角色）
- ✅ 账户状态查看

### 17. 系统设置模块 ⚙️
- ✅ 系统配置管理（前端页面管理键值对配置）
- ✅ 配置分组管理
- ✅ 配置类型管理（string、number、boolean、json等）
- ✅ 配置搜索和筛选

## 🔐 权限系统

系统采用基于角色的访问控制（RBAC）模型，提供细粒度的权限管理。

### 权限架构

```
用户 (User)
  └── 角色 (Role) - 多对多关系
      └── 权限 (Permission) - 多对多关系
```

### 权限验证机制

- ✅ **中间件验证**：所有业务接口都通过 `permission` 中间件进行权限验证
- ✅ **性能优化**：预加载 roles 和 permissions 关系，避免 N+1 查询问题
- ✅ **统一响应**：未授权访问返回统一的 403 错误响应

### 默认角色和权限

系统初始化时会创建以下角色：

| 角色 | 说明 | 权限范围 |
|------|------|----------|
| **系统管理员** | 拥有所有权限 | 全部功能模块 |
| **经理** | 业务管理权限 | 商品、库存、采购、销售、生产、报表等 |
| **操作员** | 基础操作权限 | 库存查看、入库出库、工单管理、生产报工 |

### 权限列表

系统定义了 50+ 个权限，涵盖所有业务模块：

- **系统管理**：`users.manage`, `roles.manage`, `permissions.manage`, `system.config`
- **基础数据**：`products.manage`, `product-categories.manage`, `warehouses.manage`, `suppliers.manage`, `customers.manage`
- **库存管理**：`inventory.view`, `inventory.stock-in`, `inventory.stock-out`, `inventory.transfer`, `inventory.adjust`, `inventory.stocktake`
- **采购管理**：`purchase-orders.manage`, `purchase-orders.approve`, `purchase-orders.receive`, `purchase-returns.manage`, `purchase-settlements.manage`
- **销售管理**：`sales-orders.manage`, `sales-orders.approve`, `sales-orders.ship`, `sales-returns.manage`, `sales-settlements.manage`
- **生产管理**：`boms.manage`, `process-routes.manage`, `production-plans.manage`, `work-orders.manage`, `production-reports.manage`
- **财务管理**：`chart-of-accounts.manage`, `accounting-vouchers.manage`, `accounting-vouchers.post`, `general-ledger.view`, `accounts-receivable.manage`, `accounts-payable.manage`, `financial-reports.view`
- **审批流程**：`workflows.manage`, `approvals.handle`
- **报表分析**：`sales-reports.view`, `purchase-reports.view`, `inventory-reports.view`, `custom-reports.manage`

详细权限列表请参考 `database/seeders/RolePermissionSeeder.php`。

## 🚀 快速开始

### 方式一：使用 Docker（推荐）

使用 Docker 可以快速启动完整的开发环境，无需手动安装 PHP、MySQL、Redis 等。

#### 环境要求

- Docker >= 20.10
- Docker Compose >= 2.0

#### 安装步骤

```bash
# 1. 克隆项目
git clone <repository-url>
cd laravel-erp

# 2. 复制环境配置文件
cp .env.example .env

# 3. 编辑 .env 文件，配置数据库连接
# DB_HOST=db
# DB_DATABASE=laravel_erp
# DB_USERNAME=laravel_user
# DB_PASSWORD=password

# 4. 启动所有服务
docker-compose up -d

# 5. 进入应用容器安装依赖
docker-compose exec app composer install
docker-compose exec app npm install

# 6. 生成应用密钥
docker-compose exec app php artisan key:generate

# 7. 运行数据库迁移和填充
docker-compose exec app php artisan migrate
docker-compose exec app php artisan db:seed
```

访问 `http://localhost:8000` 即可使用系统。

**常用 Docker 命令：**

```bash
# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f app

# 停止所有服务
docker-compose down

# 进入应用容器
docker-compose exec app bash
```

更多 Docker 使用说明请参考 [docker/README.md](./docker/README.md)。

---

### 方式二：传统安装

#### 环境要求

- PHP >= 8.0.2
- Composer >= 2.0
- Node.js >= 16.0
- MySQL >= 5.7 或 MariaDB >= 10.3
- Nginx 或 Apache

#### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd laravel-erp
```

#### 2. 安装依赖

```bash
# 安装 PHP 依赖
composer install

# 安装前端依赖
npm install
```

#### 3. 环境配置

```bash
# 复制环境配置文件
cp .env.example .env

# 生成应用密钥
php artisan key:generate
```

编辑 `.env` 文件，配置数据库连接：

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel_erp
DB_USERNAME=root
DB_PASSWORD=your_password
```

#### 4. 数据库迁移和填充

```bash
# 运行数据库迁移
php artisan migrate

# 填充初始数据（角色权限、系统配置、基础数据等）
php artisan db:seed
```

#### 5. 启动开发服务器

```bash
# 启动 Laravel 开发服务器（后端）
php artisan serve

# 启动 Vite 开发服务器（前端，新终端）
npm run dev
```

访问 `http://localhost:8000` 即可使用系统。

---

### 默认账户

系统初始化后会创建默认管理员账户：

- **邮箱**：admin@example.com
- **密码**：password（请在生产环境中修改）

## 📚 API 接口文档

系统提供完整的 RESTful API 接口，使用 Laravel Sanctum 进行身份认证。

### 认证接口

```http
POST   /api/login          # 用户登录
POST   /api/logout         # 用户登出（需要认证）
GET    /api/me             # 获取当前用户信息（需要认证）
```

### 主要接口模块

所有接口都需要通过 `auth:sanctum` 中间件认证，主要接口包括：

| 模块 | 接口路径 | 说明 |
|------|----------|------|
| **用户管理** | `/api/users` | 用户 CRUD 操作 |
| **角色权限** | `/api/roles`, `/api/permissions` | 角色和权限管理 |
| **商品管理** | `/api/products`, `/api/product-categories` | 商品和分类管理 |
| **库存管理** | `/api/inventory`, `/api/inventory/stock-in`, `/api/inventory/stock-out` | 库存操作 |
| **采购管理** | `/api/purchase-orders`, `/api/purchase-returns`, `/api/purchase-settlements` | 采购业务流程 |
| **销售管理** | `/api/sales-orders`, `/api/sales-returns`, `/api/sales-settlements` | 销售业务流程 |
| **生产管理** | `/api/production-plans`, `/api/work-orders`, `/api/boms` | 生产管理 |
| **财务管理** | `/api/chart-of-accounts`, `/api/accounting-vouchers`, `/api/general-ledger` | 财务管理 |
| **审批流程** | `/api/workflows`, `/api/workflow-instances`, `/api/approval-records` | 审批流程 |
| **报表分析** | `/api/sales-reports`, `/api/purchase-reports`, `/api/inventory-reports` | 报表查询 |
| **消息通知** | `/api/notifications`, `/api/notification-templates` | 消息通知和模板管理 |
| **仪表盘** | `/api/dashboard/stats`, `/api/dashboard/pending-tasks`, `/api/dashboard/recent-orders` | 仪表盘数据统计 |
| **操作日志** | `/api/operation-logs` | 操作日志查询 |

### API 文档生成

系统集成了 Scribe 用于生成 API 文档：

```bash
# 生成 API 文档
php artisan scribe:generate
```

文档将生成在 `public/docs` 目录下。

## 🗄️ 数据库结构

系统共包含 **66 张数据库表**，涵盖所有业务模块：

### 核心表结构

- **用户权限**：`users`, `roles`, `permissions`, `role_user`, `permission_role`
- **系统配置**：`system_configs`, `operation_logs`, `data_dictionaries`
- **基础数据**：`product_categories`, `units`, `currencies`, `regions`, `products`
- **仓库库存**：`warehouses`, `warehouse_locations`, `inventory`, `inventory_transactions`, `inventory_stocktakes`, `inventory_stocktake_items`
- **供应商客户**：`suppliers`, `customers`
- **采购相关**：`purchase_orders`, `purchase_order_items`, `purchase_returns`, `purchase_return_items`, `purchase_settlements`, `purchase_settlement_items`
- **销售相关**：`sales_orders`, `sales_order_items`, `sales_returns`, `sales_return_items`, `sales_settlements`, `sales_settlement_items`
- **BOM 生产**：`boms`, `bom_items`, `process_routes`, `process_route_steps`, `production_plans`, `production_plan_items`, `work_orders`, `work_order_items`, `production_material_issues`, `production_material_issue_items`, `production_reports`
- **财务相关**：`chart_of_accounts`, `accounting_vouchers`, `accounting_voucher_items`, `general_ledger`, `accounts_receivable`, `accounts_payable`, `cost_allocations`
- **审批流程**：`workflows`, `workflow_nodes`, `workflow_instances`, `approval_records`
- **报表分析**：`report_definitions`, `report_schedules`
- **消息通知**：`notifications`, `notification_templates`, `notification_logs`

所有表都包含性能优化索引，确保查询效率。

## 💻 开发指南

### 代码规范

项目使用 Laravel Pint 进行代码格式化：

```bash
# 格式化代码
./vendor/bin/pint

# 检查代码风格
./vendor/bin/pint --test
```

### 目录结构

```
laravel-erp/
├── app/
│   ├── Console/          # 命令行任务
│   ├── Exceptions/       # 异常处理
│   ├── Http/
│   │   ├── Controllers/  # 控制器（按模块分类）
│   │   ├── Middleware/   # 中间件（权限验证等）
│   │   └── Responses/    # 统一响应类
│   ├── Models/           # 模型（Eloquent ORM）
│   ├── Providers/        # 服务提供者
│   └── Services/         # 业务服务类
├── database/
│   ├── factories/        # 模型工厂
│   ├── migrations/       # 数据库迁移（66 个迁移文件）
│   └── seeders/          # 数据填充
├── resources/
│   ├── js/
│   │   ├── components/   # Vue 组件
│   │   ├── layouts/      # 布局组件
│   │   ├── views/        # 页面视图（按模块分类）
│   │   ├── services/     # API 服务
│   │   ├── stores/       # Pinia 状态管理
│   │   └── router/       # 路由配置
│   └── css/              # 样式文件
├── routes/
│   ├── api.php           # API 路由（所有接口）
│   ├── web.php           # Web 路由
│   └── channels.php      # 广播频道
└── tests/                # 测试文件
```

### 前端开发规范

系统采用统一的现代化界面设计规范：

- **页面布局**：使用统一的 `page-container` 和 `page-card` 结构
- **页面头部**：使用 `page-header`、`page-title` 和 `page-actions` 组件
- **搜索表单**：统一使用 `search-form-modern` 样式类
- **数据表格**：使用 `modern-table` 包装，提供统一的表格样式
- **分页组件**：使用 `modern-pagination` 包装
- **交互按钮**：统一使用 `interactive` 类
- **图标使用**：统一使用 Lucide Vue Next 图标库

### 测试

```bash
# 运行所有测试
php artisan test

# 运行特定测试文件
php artisan test --filter UserTest
```

### 添加新功能

1. **创建数据库迁移**：`php artisan make:migration create_xxx_table`
2. **创建模型**：`php artisan make:model ModelName`
3. **创建控制器**：`php artisan make:controller Api/ControllerName`
4. **添加路由**：在 `routes/api.php` 中添加路由，并配置权限中间件
5. **创建前端页面**：在 `resources/js/views/` 下创建 Vue 组件
6. **添加权限**：在 `RolePermissionSeeder` 中添加新权限

## 🔒 安全说明

### 认证和授权

- ✅ 所有 API 接口使用 Laravel Sanctum 进行身份认证
- ✅ 基于角色的访问控制（RBAC），支持细粒度权限管理
- ✅ 所有业务接口都通过 `permission` 中间件进行权限验证
- ✅ 未授权访问返回统一的 403 错误响应

### 数据安全

- ✅ 所有用户操作自动记录在操作日志中（`operation_logs` 表）
  - ✅ 自动记录所有 API 操作（POST、PUT、PATCH、DELETE）
  - ✅ 完整记录请求数据和响应数据，便于审计和问题排查
  - ✅ 自动排除敏感信息（密码、token 等）
  - ✅ 支持前端页面查询和管理操作日志
- ✅ 敏感数据使用加密存储
- ✅ SQL 注入防护（使用 Eloquent ORM 参数绑定）
- ✅ XSS 防护（Vue 3 自动转义）

### 性能安全

- ✅ 数据库查询优化（避免 N+1 查询问题）
- ✅ 权限检查性能优化（预加载关系）
- ✅ 数据库索引优化（关键字段建立索引）

## 📝 默认数据

系统初始化时会自动填充以下数据：

- ✅ **角色权限数据**：管理员、经理、操作员等角色及相应权限（50+ 个权限）
- ✅ **系统配置数据**：系统基础配置项
- ✅ **基础数据**：计量单位、币种、地区、仓库等基础数据
- ✅ **会计科目数据**：标准会计科目表（支持树形结构）
- ✅ **用户数据**：默认管理员账户


### 快速部署

```bash
# 1. 克隆项目
git clone <repository-url>
cd laravel-erp

# 2. 安装依赖
composer install --optimize-autoloader --no-dev
npm install && npm run build

# 3. 配置环境
cp .env.example .env
php artisan key:generate

# 4. 数据库迁移和填充
php artisan migrate --force
php artisan db:seed --force

# 5. 优化
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

## 📄 许可证

本项目采用 [MIT 许可证](https://opensource.org/licenses/MIT)。

## 📅 更新日志

### 最新更新

- ✅ **操作日志自动记录**：实现操作日志自动记录功能
  - ✅ 创建操作日志中间件，自动记录所有 API 操作
  - ✅ 自动识别操作模块和操作动作（创建、更新、删除、审核、提交、取消等）
  - ✅ 自动排除敏感信息（密码、token 等显示为 `***`）
  - ✅ 自动排除查询操作（GET 请求）以提升性能
  - ✅ 完整记录请求数据和响应数据（不截断，JSON 格式化）
  - ✅ 前端页面查询和管理（系统管理 → 操作日志）
  - ✅ 详情页面优化（数据自动换行，支持查看完整请求和响应数据）
- ✅ **文档完善**：补充遗漏功能模块说明
  - ✅ 新增仪表盘模块说明（数据统计、趋势分析、待处理事项、快捷操作）
  - ✅ 新增个人中心模块说明（个人信息管理、密码修改）
  - ✅ 新增系统设置模块说明（系统配置前端管理）
  - ✅ 完善API接口文档（添加仪表盘和操作日志接口）
- ✅ **消息模板管理功能**：新增消息模板管理前端页面
  - ✅ 支持通过前端页面配置消息模板（系统管理 → 消息模板）
  - ✅ 支持多种消息类型（系统消息、审批、订单、库存、财务）
  - ✅ 支持多种发送渠道（系统内消息、邮件、短信、推送）
  - ✅ 模板变量功能（使用 `{变量名}` 格式，支持动态替换）
  - ✅ 模板预览功能（支持测试数据预览）
  - ✅ 模板启用/禁用控制
  - ✅ 消息发送日志记录（记录所有发送渠道的发送状态）

### 2025-12-22

- ✅ **权限系统完善**：为所有业务接口添加权限验证中间件
- ✅ **性能优化**：优化 User 模型的权限检查方法，避免 N+1 查询问题
- ✅ **权限中间件优化**：预加载 roles 和 permissions 关系，提升性能
- ✅ **数据库优化**：添加性能索引，优化查询效率

### 2025-12-18

- ✅ **核心功能完成**：完成所有 15 个核心功能模块开发
- ✅ **界面统一**：统一所有页面布局和样式，采用现代化设计规范
- ✅ **权限系统**：实现基于 RBAC 的权限管理系统
- ✅ **API 文档**：集成 Scribe 生成 API 文档

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

### 贡献指南

1. Fork 本项目
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📞 联系方式

如有问题或建议，请通过以下方式联系：

- 📧 提交 [Issue](https://github.com/your-repo/laravel-erp/issues)
- 📖 查看 [文档](./DEPLOYMENT.md)

---

<div align="center">

**Made with ❤️ using Laravel & Vue**

[⬆ 回到顶部](#laravel-erp-系统)

</div>
